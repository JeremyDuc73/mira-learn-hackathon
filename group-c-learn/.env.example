# ═══════════════════════════════════════════════════════════════════
# Groupe C — Front public learn : catalogue + profil étudiant + parcours
# Credentials Supabase branche groupe-c + OpenRouter dédié ($5 budget)
# ═══════════════════════════════════════════════════════════════════

# ─── Identité du service ─────────────────────────────────────────
SERVICE_NAME=group-c-learn-api
BUILD_SHA=dev
BUILD_DATE=2026-05-11
ENVIRONMENT=development
LOG_LEVEL=INFO

# ─── Base de données ─────────────────────────────────────────────
# Supabase branche groupe-c
# Postgres local lancé via `docker compose up -d` (cf. docker-compose.yml).
# Supabase est utilisé pour l'Auth uniquement.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5434/postgres

# ─── Supabase ────────────────────────────────────────────────────
SUPABASE_URL=https://REPLACE_WITH_SUPABASE_BRANCH_URL.supabase.co
SUPABASE_ANON_KEY=sb_publishable_REPLACE_WITH_GROUP_KEY
SUPABASE_JWKS_URL=

SUPABASE_SERVICE_ROLE_KEY=

# ─── OpenRouter (LLM) ────────────────────────────────────────────
# Clé dédiée Groupe C — budget cap $5
OPENROUTER_API_KEY=sk-or-v1-REPLACE_WITH_GROUP_OPENROUTER_KEY
OPENROUTER_DEFAULT_MODEL=anthropic/claude-3.5-haiku

# ═══════════════════════════════════════════════════════════════════
# FRONTEND Next.js
# ═══════════════════════════════════════════════════════════════════

NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_URL=https://REPLACE_WITH_SUPABASE_BRANCH_URL.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_REPLACE_WITH_GROUP_KEY
